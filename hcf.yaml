#!/usr/bin/python
def main():
  rem = 1 
  numbers = sorted([int(x) for x in raw_input().split()])
  if numbers[0] == numbers[1]:
    print numbers[0]
  if numbers[0] != numbers[1]:
    while rem > 0:
      rem = numbers[1] % numbers[0]
      numbers[1] = numbers[0]
      numbers[0] = rem
    print numbers[1]
main()
